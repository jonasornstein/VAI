# Outbox (AIRUP — **P**)

Approved artifacts only. Kricke and ornstein use **`outbox/proposals/`** on race day.

| Subfolder | Contents |
|-----------|----------|
| `proposals/` | Approved betting systems (`v85/<date>-<track>/`) |
| `mockups/` | Approved UX mockups (canonical: `v85-proposal-ux-mockup-atg.html` v0.5) |
| `research/` | Approved research outputs |
| `reviews/` | Signed review records |

**Gate:** Nothing enters `outbox/` without `APPROVED` status in `pending/`.